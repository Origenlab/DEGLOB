---
import Base from '../../../../layouts/Base.astro';
import Hero from '../../../../components/Hero.astro';
import Breadcrumb from '../../../../components/Breadcrumb.astro';
import CtaBar from '../../../../components/CtaBar.astro';
import FaqSection from '../../../../components/FaqSection.astro';
import { SERVICIOS, WHATSAPP_URL_MSG, SITE_URL } from '../../../../lib/config';
import { faqSchema } from '../../../../lib/seo';

const servicio = SERVICIOS.find(s => s.slug === 'figuras-personalizadas')!;
const paquete = servicio.paquetes.find(p => p.slug === 'letras-nombre')!;
const otrosPaquetes = servicio.paquetes.filter(p => p.slug !== 'letras-nombre');

const faqItems = [
  { pregunta: '¿Cuántas letras incluye el paquete?', respuesta: 'El paquete incluye hasta 6 letras de 80cm de altura cada una. Para nombres más largos podemos agregar letras adicionales por un costo extra por letra.' },
  { pregunta: '¿Qué tipo de nombres son los más solicitados?', respuesta: 'Los más populares son nombres de festejados para cumpleaños, nombres de bebés para baby showers, nombres de novios para bodas, y palabras como "AMOR", "BABY" o "FIESTA" para eventos temáticos.' },
  { pregunta: '¿Las letras se pueden colgar o solo van de pie?', respuesta: 'Las letras incluyen estructura autoportante para colocarse de pie sobre mesas o superficies. También podemos adaptarlas para colgar en paredes o desde el techo si lo prefieres.' },
  { pregunta: '¿En cuántos colores puedo hacer las letras?', respuesta: 'Puedes elegir un color uniforme para todas las letras o usar un color diferente para cada una. Las opciones incluyen toda nuestra gama de globos latex, chrome y pastel.' },
  { pregunta: '¿Las letras incluyen estructura?', respuesta: 'Sí, cada letra tiene una estructura autoportante de alambre que define la forma con precisión. Los globos se adhieren sobre esta estructura creando letras definidas y consistentes.' },
];

const structuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: `${paquete.nombre} — ${servicio.nombre}`,
    description: `Paquete ${paquete.nombre} de ${servicio.nombre.toLowerCase()}. ${servicio.descripcion}`,
    brand: { '@type': 'Organization', name: 'DEGLOB' },
    offers: {
      '@type': 'Offer',
      price: paquete.precio,
      priceCurrency: 'MXN',
      availability: 'https://schema.org/InStock',
      url: `${SITE_URL}/servicios/${servicio.slug}/${paquete.slug}/`,
    },
    aggregateRating: {
      '@type': 'AggregateRating',
      ratingValue: '4.9',
      reviewCount: '45',
      bestRating: '5',
    },
  },
  faqSchema(faqItems),
];
---

<Base
  title={`${paquete.nombre} — ${servicio.nombre} | DEGLOB`}
  description={`${paquete.nombre} de ${servicio.nombre.toLowerCase()} desde $${paquete.precio.toLocaleString()} MXN. ${paquete.features.slice(0, 3).join(', ')}. Instalación incluida en CDMX.`}
  structuredData={structuredData}
>
  <Breadcrumb items={[
    { name: 'Servicios', href: '/servicios/' },
    { name: servicio.nombre, href: `/servicios/${servicio.slug}/` },
    { name: paquete.nombre, href: `/servicios/${servicio.slug}/${paquete.slug}/` },
  ]} />

  <Hero
    badge={`${servicio.nombre} — ${paquete.nombre}`}
    title={`${paquete.nombre}`}
    description={`Paquete de ${servicio.nombre.toLowerCase()} que incluye ${paquete.features[0].toLowerCase()}. Instalación profesional incluida en toda la CDMX.`}
    primaryCta={{ text: `Cotizar $${paquete.precio.toLocaleString()} + IVA`, href: WHATSAPP_URL_MSG }}
    secondaryCta={{ text: 'Ver más paquetes', href: `/servicios/${servicio.slug}/` }}
  >
    <Fragment slot="hero-seo">
      <p>Las letras y nombres hechos con globos son la personalización definitiva para cualquier evento en CDMX. Con hasta 6 letras de 80cm de altura cada una, creamos nombres, palabras y mensajes que se convierten en la decoración más fotografiada de tu celebración.</p>
      <p>Cada letra se construye sobre estructura autoportante con globos premium en los colores que elijas. Desde el nombre del bebé en un baby shower hasta los nombres de los novios en una boda, estas letras agregan un toque personal único que identifica tu evento.</p>
    </Fragment>
  </Hero>

  <CtaBar
    items={otrosPaquetes.map(p => ({ label: p.nombre, href: `/servicios/${servicio.slug}/${p.slug}/` }))}
    headline="Otros paquetes disponibles"
  />

  <section class="section">
    <div class="container">
      <div class="features-grid">
        {paquete.features.map(f => (
          <div class="feature-item feature-item--yes">
            <span class="feature-item__icon">&#10003;</span>
            <span>{f}</span>
          </div>
        ))}
      </div>

      <div class="price-highlight">
        <span class="price-highlight__label">Precio desde</span>
        <span class="price-highlight__amount">${paquete.precio.toLocaleString()} <small>MXN + IVA</small></span>
        <span class="price-highlight__note">Instalación incluida en CDMX</span>
        <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener" style="margin-top:1rem">Reservar Ahora</a>
      </div>
    </div>
  </section>

  <section class="section section--bg">
    <div class="container">
      <div class="prose" style="max-width:800px">
        <h2>Letras y Nombres de Globos: Personalización Única para tu Evento</h2>
        <p>Las letras y nombres hechos con globos son una de las tendencias decorativas más fuertes en eventos de la Ciudad de México. Con 80cm de altura por letra y hasta 6 caracteres incluidos, puedes deletrear el nombre del festejado, una palabra especial o un mensaje que defina la esencia de tu celebración.</p>
        <p>Cada letra se elabora sobre una estructura autoportante de alambre que le da forma precisa y estabilidad. Los globos se adhieren creando una textura volumétrica que da vida a cada carácter. Puedes elegir un color uniforme para un look elegante o múltiples colores para un efecto más festivo.</p>
        <p>Las letras de globos son especialmente populares en baby showers donde se deletrea el nombre del bebé, en cumpleaños donde se muestra la edad y nombre del festejado, y en bodas donde los nombres de los novios decoran la mesa principal o el photobooth.</p>
      </div>
    </div>
  </section>

  <FaqSection items={faqItems} />

  <section class="cta-dark">
    <div class="container">
      <h2 class="cta-dark__title">¿Listo para reservar?</h2>
      <p class="cta-dark__text">Cotiza tu {paquete.nombre.toLowerCase()} y haz tu evento inolvidable.</p>
      <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener">Cotizar por WhatsApp</a>
    </div>
  </section>
</Base>

<style>
  .features-grid { display: grid; grid-template-columns: 1fr; gap: 0.75rem; max-width: 600px; margin: 0 auto var(--spacing-2xl); }
  .feature-item { display: flex; align-items: center; gap: 0.75rem; padding: 0.875rem 1rem; border-radius: var(--border-radius); font-size: var(--font-size-sm); }
  .feature-item--yes { background: #f0fdf4; color: #166534; }
  .feature-item__icon { font-weight: 700; flex-shrink: 0; }
  .price-highlight { text-align: center; padding: var(--spacing-xl); background: var(--color-bg); border-radius: var(--border-radius-lg); max-width: 500px; margin: 0 auto; display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .price-highlight__label { font-size: var(--font-size-sm); color: var(--color-text-light); text-transform: uppercase; letter-spacing: 0.05em; font-weight: 600; }
  .price-highlight__amount { font-size: 2.5rem; font-weight: 800; color: var(--color-primary); line-height: 1.2; }
  .price-highlight__amount small { font-size: 1rem; font-weight: 500; color: var(--color-text-light); }
  .price-highlight__note { font-size: var(--font-size-sm); color: var(--color-text-light); }
  @media (min-width: 640px) { .features-grid { grid-template-columns: repeat(2, 1fr); } }
</style>
