---
import Base from '../../../../layouts/Base.astro';
import Hero from '../../../../components/Hero.astro';
import Breadcrumb from '../../../../components/Breadcrumb.astro';
import CtaBar from '../../../../components/CtaBar.astro';
import FaqSection from '../../../../components/FaqSection.astro';
import { SERVICIOS, WHATSAPP_URL_MSG, SITE_URL } from '../../../../lib/config';
import { faqSchema } from '../../../../lib/seo';

const servicio = SERVICIOS.find(s => s.slug === 'figuras-personalizadas')!;
const paquete = servicio.paquetes.find(p => p.slug === 'produccion-figuras')!;
const otrosPaquetes = servicio.paquetes.filter(p => p.slug !== 'produccion-figuras');

const faqItems = [
  { pregunta: '¿Qué incluye la producción completa de figuras?', respuesta: 'Incluye múltiples figuras personalizadas, diseño integral temático, figuras en todos los tamaños necesarios, instalación y desmontaje profesional, y asesoría creativa para definir el concepto visual completo.' },
  { pregunta: '¿Cuántas figuras incluye la producción completa?', respuesta: 'La producción incluye 3 o más figuras de diferentes tamaños. Puede ser una combinación de números, personajes, letras y formas temáticas que juntas cuenten una historia visual coherente.' },
  { pregunta: '¿Pueden crear una temática completa solo con figuras?', respuesta: 'Sí, podemos diseñar una decoración completa basada en figuras: por ejemplo, un castillo de princesas con torres, un escenario del espacio con cohetes y planetas, o un mundo marino con figuras acuáticas.' },
  { pregunta: '¿El diseño creativo tiene costo adicional?', respuesta: 'No, la asesoría creativa y el diseño están incluidos en el precio de $13,000 MXN. Nos reunimos contigo para entender la temática y crear una propuesta visual antes de la producción.' },
  { pregunta: '¿Cuánto tiempo antes se necesita reservar?', respuesta: 'Recomendamos reservar con mínimo 2 semanas de anticipación para producciones completas de figuras. Esto nos permite planificar los diseños, conseguir materiales especiales y coordinar el montaje.' },
];

const structuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: `${paquete.nombre} — ${servicio.nombre}`,
    description: `Paquete ${paquete.nombre} de ${servicio.nombre.toLowerCase()}. ${servicio.descripcion}`,
    brand: { '@type': 'Organization', name: 'DEGLOB' },
    offers: {
      '@type': 'Offer',
      price: paquete.precio,
      priceCurrency: 'MXN',
      availability: 'https://schema.org/InStock',
      url: `${SITE_URL}/servicios/${servicio.slug}/${paquete.slug}/`,
    },
    aggregateRating: {
      '@type': 'AggregateRating',
      ratingValue: '4.9',
      reviewCount: '30',
      bestRating: '5',
    },
  },
  faqSchema(faqItems),
];
---

<Base
  title={`${paquete.nombre} — ${servicio.nombre} | DEGLOB`}
  description={`${paquete.nombre} de ${servicio.nombre.toLowerCase()} desde $${paquete.precio.toLocaleString()} MXN. ${paquete.features.slice(0, 3).join(', ')}. Instalación incluida en CDMX.`}
  structuredData={structuredData}
>
  <Breadcrumb items={[
    { name: 'Servicios', href: '/servicios/' },
    { name: servicio.nombre, href: `/servicios/${servicio.slug}/` },
    { name: paquete.nombre, href: `/servicios/${servicio.slug}/${paquete.slug}/` },
  ]} />

  <Hero
    badge={`${servicio.nombre} — ${paquete.nombre}`}
    title={`${paquete.nombre}`}
    description={`Paquete de ${servicio.nombre.toLowerCase()} que incluye ${paquete.features[0].toLowerCase()}. Instalación profesional incluida en toda la CDMX.`}
    primaryCta={{ text: `Cotizar $${paquete.precio.toLocaleString()} + IVA`, href: WHATSAPP_URL_MSG }}
    secondaryCta={{ text: 'Ver más paquetes', href: `/servicios/${servicio.slug}/` }}
  >
    <Fragment slot="hero-seo">
      <p>La producción completa de figuras personalizadas es nuestro servicio creativo más ambicioso: múltiples figuras temáticas, diseño integral y asesoría artística para crear un mundo visual completo con globos en tu evento en CDMX.</p>
      <p>Desde escenarios de fantasía con castillos y personajes hasta ambientaciones espaciales con cohetes y planetas, este paquete transforma cualquier espacio en un universo temático construido enteramente con globos premium. Incluye instalación profesional y desmontaje.</p>
    </Fragment>
  </Hero>

  <CtaBar
    items={otrosPaquetes.map(p => ({ label: p.nombre, href: `/servicios/${servicio.slug}/${p.slug}/` }))}
    headline="Otros paquetes disponibles"
  />

  <section class="section">
    <div class="container">
      <div class="features-grid">
        {paquete.features.map(f => (
          <div class="feature-item feature-item--yes">
            <span class="feature-item__icon">&#10003;</span>
            <span>{f}</span>
          </div>
        ))}
      </div>

      <div class="price-highlight">
        <span class="price-highlight__label">Precio desde</span>
        <span class="price-highlight__amount">${paquete.precio.toLocaleString()} <small>MXN + IVA</small></span>
        <span class="price-highlight__note">Instalación incluida en CDMX</span>
        <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener" style="margin-top:1rem">Reservar Ahora</a>
      </div>
    </div>
  </section>

  <section class="section section--bg">
    <div class="container">
      <div class="prose" style="max-width:800px">
        <h2>Producción Completa de Figuras de Globos: Un Mundo Temático para tu Evento</h2>
        <p>La producción completa de figuras de globos es donde la creatividad no tiene límites. Este servicio integral crea un mundo temático completo con múltiples figuras que transforman tu venue en un escenario de fantasía que encanta a niños y adultos por igual.</p>
        <p>El proceso creativo comienza con una sesión de asesoría donde definimos la temática y diseñamos una propuesta visual. Nuestro equipo de escultores de globos trabaja durante varios días creando cada pieza, desde personajes principales de gran tamaño hasta elementos decorativos complementarios.</p>
        <p>Las producciones más solicitadas incluyen mundos de princesas con castillo y personajes, escenarios espaciales con cohetes y planetas, junglas con animales tropicales, y mundos marinos con figuras acuáticas. Todo se transporta e instala profesionalmente en cualquier salón de la CDMX.</p>
      </div>
    </div>
  </section>

  <FaqSection items={faqItems} />

  <section class="cta-dark">
    <div class="container">
      <h2 class="cta-dark__title">¿Listo para reservar?</h2>
      <p class="cta-dark__text">Cotiza tu {paquete.nombre.toLowerCase()} y haz tu evento inolvidable.</p>
      <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener">Cotizar por WhatsApp</a>
    </div>
  </section>
</Base>

<style>
  .features-grid { display: grid; grid-template-columns: 1fr; gap: 0.75rem; max-width: 600px; margin: 0 auto var(--spacing-2xl); }
  .feature-item { display: flex; align-items: center; gap: 0.75rem; padding: 0.875rem 1rem; border-radius: var(--border-radius); font-size: var(--font-size-sm); }
  .feature-item--yes { background: #f0fdf4; color: #166534; }
  .feature-item__icon { font-weight: 700; flex-shrink: 0; }
  .price-highlight { text-align: center; padding: var(--spacing-xl); background: var(--color-bg); border-radius: var(--border-radius-lg); max-width: 500px; margin: 0 auto; display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .price-highlight__label { font-size: var(--font-size-sm); color: var(--color-text-light); text-transform: uppercase; letter-spacing: 0.05em; font-weight: 600; }
  .price-highlight__amount { font-size: 2.5rem; font-weight: 800; color: var(--color-primary); line-height: 1.2; }
  .price-highlight__amount small { font-size: 1rem; font-weight: 500; color: var(--color-text-light); }
  .price-highlight__note { font-size: var(--font-size-sm); color: var(--color-text-light); }
  @media (min-width: 640px) { .features-grid { grid-template-columns: repeat(2, 1fr); } }
</style>
