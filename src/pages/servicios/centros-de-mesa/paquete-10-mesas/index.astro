---
import Base from '../../../../layouts/Base.astro';
import Hero from '../../../../components/Hero.astro';
import Breadcrumb from '../../../../components/Breadcrumb.astro';
import CtaBar from '../../../../components/CtaBar.astro';
import FaqSection from '../../../../components/FaqSection.astro';
import { SERVICIOS, WHATSAPP_URL_MSG, SITE_URL } from '../../../../lib/config';
import { faqSchema } from '../../../../lib/seo';

const servicio = SERVICIOS.find(s => s.slug === 'centros-de-mesa')!;
const paquete = servicio.paquetes.find(p => p.slug === 'paquete-10-mesas')!;
const otrosPaquetes = servicio.paquetes.filter(p => p.slug !== 'paquete-10-mesas');

const faqItems = [
  { pregunta: '¿Qué incluye cada centro de mesa de globos?', respuesta: 'Cada centro incluye un arreglo de 3-5 globos latex y foil sobre un peso decorativo, con cintas coordinadas y colores que combinan con la temática de tu evento. Se colocan directamente sobre cada mesa.' },
  { pregunta: '¿Los centros de mesa son todos iguales?', respuesta: 'Por defecto, los 10 centros son del mismo diseño para mantener coherencia visual. Sin embargo, podemos hacer variaciones sutiles en color o forma si lo prefieres.' },
  { pregunta: '¿Ocupan mucho espacio en la mesa?', respuesta: 'No, nuestros centros de mesa están diseñados para ser compactos en su base (aproximadamente 15-20cm de diámetro) mientras los globos se elevan verticalmente, dejando espacio libre para platos y cubiertos.' },
  { pregunta: '¿Puedo combinar globos de latex con foil?', respuesta: 'Sí, la mayoría de nuestros centros de mesa combinan globos de latex con un globo foil temático (estrella, corazón, número o forma especial) que le da un toque especial al arreglo.' },
  { pregunta: '¿Cuánto tiempo antes del evento los instalan?', respuesta: 'Instalamos los centros de mesa entre 1 y 2 horas antes del evento. El equipo coloca cada arreglo en la mesa correspondiente y verifica que todos estén en perfecto estado.' },
];

const structuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: `${paquete.nombre} — ${servicio.nombre}`,
    description: `Paquete ${paquete.nombre} de ${servicio.nombre.toLowerCase()}. ${servicio.descripcion}`,
    brand: { '@type': 'Organization', name: 'DEGLOB' },
    offers: {
      '@type': 'Offer',
      price: paquete.precio,
      priceCurrency: 'MXN',
      availability: 'https://schema.org/InStock',
      url: `${SITE_URL}/servicios/${servicio.slug}/${paquete.slug}/`,
    },
    aggregateRating: {
      '@type': 'AggregateRating',
      ratingValue: '4.9',
      reviewCount: '50',
      bestRating: '5',
    },
  },
  faqSchema(faqItems),
];
---

<Base
  title={`${paquete.nombre} — ${servicio.nombre} | DEGLOB`}
  description={`${paquete.nombre} de ${servicio.nombre.toLowerCase()} desde $${paquete.precio.toLocaleString()} MXN. ${paquete.features.slice(0, 3).join(', ')}. Instalación incluida en CDMX.`}
  structuredData={structuredData}
>
  <Breadcrumb items={[
    { name: 'Servicios', href: '/servicios/' },
    { name: servicio.nombre, href: `/servicios/${servicio.slug}/` },
    { name: paquete.nombre, href: `/servicios/${servicio.slug}/${paquete.slug}/` },
  ]} />

  <Hero
    badge={`${servicio.nombre} — ${paquete.nombre}`}
    title={`${paquete.nombre}`}
    description={`Paquete de ${servicio.nombre.toLowerCase()} que incluye ${paquete.features[0].toLowerCase()}. Instalación profesional incluida en toda la CDMX.`}
    primaryCta={{ text: `Cotizar $${paquete.precio.toLocaleString()} + IVA`, href: WHATSAPP_URL_MSG }}
    secondaryCta={{ text: 'Ver más paquetes', href: `/servicios/${servicio.slug}/` }}
  >
    <Fragment slot="hero-seo">
      <p>El paquete de 10 centros de mesa con globos es la opción ideal para eventos íntimos de hasta 100 invitados en CDMX. Cada arreglo combina globos latex y foil en colores coordinados sobre pesos decorativos que complementan la decoración de tus mesas.</p>
      <p>Nuestros centros de mesa se diseñan para ser elegantes y funcionales: compactos en la base para no interferir con el servicio de alimentos, pero visualmente impactantes con globos que se elevan creando puntos de color y textura en cada mesa de tu evento.</p>
    </Fragment>
  </Hero>

  <CtaBar
    items={otrosPaquetes.map(p => ({ label: p.nombre, href: `/servicios/${servicio.slug}/${p.slug}/` }))}
    headline="Otros paquetes disponibles"
  />

  <section class="section">
    <div class="container">
      <div class="features-grid">
        {paquete.features.map(f => (
          <div class="feature-item feature-item--yes">
            <span class="feature-item__icon">&#10003;</span>
            <span>{f}</span>
          </div>
        ))}
      </div>

      <div class="price-highlight">
        <span class="price-highlight__label">Precio desde</span>
        <span class="price-highlight__amount">${paquete.precio.toLocaleString()} <small>MXN + IVA</small></span>
        <span class="price-highlight__note">Instalación incluida en CDMX</span>
        <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener" style="margin-top:1rem">Reservar Ahora</a>
      </div>
    </div>
  </section>

  <section class="section section--bg">
    <div class="container">
      <div class="prose" style="max-width:800px">
        <h2>Centros de Mesa con Globos para 10 Mesas: Elegancia en cada Detalle</h2>
        <p>Los centros de mesa con globos son el complemento perfecto para unificar la decoración de tu evento desde cada mesa. Con el paquete de 10 mesas, cubrimos eventos íntimos como baby showers, bautizos, cenas de cumpleaños y reuniones familiares donde cada detalle cuenta.</p>
        <p>Cada centro de mesa se diseña con una base compacta que no interfiere con el servicio de alimentos. Los globos se elevan verticalmente combinando texturas de latex y foil en la paleta de colores de tu evento, creando puntos de color y movimiento que alegran el ambiente de cada mesa.</p>
        <p>Este paquete es el más solicitado para eventos en restaurantes de la Condesa, jardines de Coyoacán y salones íntimos de Polanco. La instalación profesional está incluida: nuestro equipo coloca cada centro en su mesa correspondiente y verifica que todo esté perfecto.</p>
      </div>
    </div>
  </section>

  <FaqSection items={faqItems} />

  <section class="cta-dark">
    <div class="container">
      <h2 class="cta-dark__title">¿Listo para reservar?</h2>
      <p class="cta-dark__text">Cotiza tu {paquete.nombre.toLowerCase()} y haz tu evento inolvidable.</p>
      <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener">Cotizar por WhatsApp</a>
    </div>
  </section>
</Base>

<style>
  .features-grid { display: grid; grid-template-columns: 1fr; gap: 0.75rem; max-width: 600px; margin: 0 auto var(--spacing-2xl); }
  .feature-item { display: flex; align-items: center; gap: 0.75rem; padding: 0.875rem 1rem; border-radius: var(--border-radius); font-size: var(--font-size-sm); }
  .feature-item--yes { background: #f0fdf4; color: #166534; }
  .feature-item__icon { font-weight: 700; flex-shrink: 0; }
  .price-highlight { text-align: center; padding: var(--spacing-xl); background: var(--color-bg); border-radius: var(--border-radius-lg); max-width: 500px; margin: 0 auto; display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .price-highlight__label { font-size: var(--font-size-sm); color: var(--color-text-light); text-transform: uppercase; letter-spacing: 0.05em; font-weight: 600; }
  .price-highlight__amount { font-size: 2.5rem; font-weight: 800; color: var(--color-primary); line-height: 1.2; }
  .price-highlight__amount small { font-size: 1rem; font-weight: 500; color: var(--color-text-light); }
  .price-highlight__note { font-size: var(--font-size-sm); color: var(--color-text-light); }
  @media (min-width: 640px) { .features-grid { grid-template-columns: repeat(2, 1fr); } }
</style>
