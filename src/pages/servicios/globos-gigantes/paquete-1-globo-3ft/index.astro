---
import Base from '../../../../layouts/Base.astro';
import Hero from '../../../../components/Hero.astro';
import Breadcrumb from '../../../../components/Breadcrumb.astro';
import CtaBar from '../../../../components/CtaBar.astro';
import FaqSection from '../../../../components/FaqSection.astro';
import { SERVICIOS, WHATSAPP_URL_MSG, SITE_URL } from '../../../../lib/config';
import { faqSchema } from '../../../../lib/seo';

const servicio = SERVICIOS.find(s => s.slug === 'globos-gigantes')!;
const paquete = servicio.paquetes.find(p => p.slug === 'paquete-1-globo-3ft')!;
const otrosPaquetes = servicio.paquetes.filter(p => p.slug !== 'paquete-1-globo-3ft');

const faqItems = [
  { pregunta: '¿Qué tamaño real tiene un globo gigante de 3 pies?', respuesta: 'Un globo gigante de 3 pies (36 pulgadas) tiene aproximadamente 90cm de diámetro cuando está inflado. Es significativamente más grande que un globo estándar y crea un impacto visual inmediato.' },
  { pregunta: '¿Qué tipo de confeti se usa como relleno?', respuesta: 'Utilizamos confeti de papel metalizado en colores dorado, plateado, rosa gold, multicolor y más. También podemos usar confeti personalizado con formas de estrellas, corazones o círculos.' },
  { pregunta: '¿Las borlas decorativas están incluidas?', respuesta: 'Sí, el paquete incluye borlas (tassels) de papel de seda que cuelgan de la parte inferior del globo, agregando movimiento y color. Puedes elegir los colores de las borlas.' },
  { pregunta: '¿El globo gigante se infla con helio o con aire?', respuesta: 'Los globos gigantes se inflan con helio para que floten, o con aire si prefieres que se sostenga en un pedestal o estructura. La opción con helio es la más popular para un efecto flotante espectacular.' },
  { pregunta: '¿Ofrecen entrega a domicilio del globo gigante?', respuesta: 'Sí, la entrega a domicilio está incluida en toda la CDMX. Transportamos el globo inflado y listo en nuestro vehículo especial para que llegue en perfectas condiciones a tu evento.' },
];

const structuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: `${paquete.nombre} — ${servicio.nombre}`,
    description: `Paquete ${paquete.nombre} de ${servicio.nombre.toLowerCase()}. ${servicio.descripcion}`,
    brand: { '@type': 'Organization', name: 'DEGLOB' },
    offers: {
      '@type': 'Offer',
      price: paquete.precio,
      priceCurrency: 'MXN',
      availability: 'https://schema.org/InStock',
      url: `${SITE_URL}/servicios/${servicio.slug}/${paquete.slug}/`,
    },
    aggregateRating: {
      '@type': 'AggregateRating',
      ratingValue: '4.9',
      reviewCount: '68',
      bestRating: '5',
    },
  },
  faqSchema(faqItems),
];
---

<Base
  title={`${paquete.nombre} — ${servicio.nombre} | DEGLOB`}
  description={`${paquete.nombre} de ${servicio.nombre.toLowerCase()} desde $${paquete.precio.toLocaleString()} MXN. ${paquete.features.slice(0, 3).join(', ')}. Instalación incluida en CDMX.`}
  structuredData={structuredData}
>
  <Breadcrumb items={[
    { name: 'Servicios', href: '/servicios/' },
    { name: servicio.nombre, href: `/servicios/${servicio.slug}/` },
    { name: paquete.nombre, href: `/servicios/${servicio.slug}/${paquete.slug}/` },
  ]} />

  <Hero
    badge={`${servicio.nombre} — ${paquete.nombre}`}
    title={`${paquete.nombre}`}
    description={`Paquete de ${servicio.nombre.toLowerCase()} que incluye ${paquete.features[0].toLowerCase()}. Instalación profesional incluida en toda la CDMX.`}
    primaryCta={{ text: `Cotizar $${paquete.precio.toLocaleString()} + IVA`, href: WHATSAPP_URL_MSG }}
    secondaryCta={{ text: 'Ver más paquetes', href: `/servicios/${servicio.slug}/` }}
  >
    <Fragment slot="hero-seo">
      <p>El globo gigante de 3 pies con confeti es la pieza decorativa de mayor impacto visual por unidad. Con 90cm de diámetro, relleno de confeti metálico, borlas decorativas y cinta premium, este globo se convierte en el protagonista de cualquier celebración en CDMX.</p>
      <p>Perfecto como regalo sorpresa, pieza central de decoración o elemento fotogénico para sesiones de fotos. Incluye entrega a domicilio en toda la Ciudad de México con el globo inflado y listo para sorprender.</p>
    </Fragment>
  </Hero>

  <CtaBar
    items={otrosPaquetes.map(p => ({ label: p.nombre, href: `/servicios/${servicio.slug}/${p.slug}/` }))}
    headline="Otros paquetes disponibles"
  />

  <section class="section">
    <div class="container">
      <div class="features-grid">
        {paquete.features.map(f => (
          <div class="feature-item feature-item--yes">
            <span class="feature-item__icon">&#10003;</span>
            <span>{f}</span>
          </div>
        ))}
      </div>

      <div class="price-highlight">
        <span class="price-highlight__label">Precio desde</span>
        <span class="price-highlight__amount">${paquete.precio.toLocaleString()} <small>MXN + IVA</small></span>
        <span class="price-highlight__note">Instalación incluida en CDMX</span>
        <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener" style="margin-top:1rem">Reservar Ahora</a>
      </div>
    </div>
  </section>

  <section class="section section--bg">
    <div class="container">
      <div class="prose" style="max-width:800px">
        <h2>Globo Gigante de 3 Pies: Impacto Visual Máximo</h2>
        <p>El globo gigante de 3 pies (90cm de diámetro) es una declaración decorativa por sí mismo. Con su tamaño imponente, relleno de confeti metálico que brilla con la luz y borlas decorativas que cuelgan elegantemente, este globo crea un punto focal instantáneo en cualquier espacio.</p>
        <p>Es la opción perfecta para regalos sorpresa de cumpleaños, propuestas de matrimonio, anuncios de embarazo, decoración de restaurantes para cenas especiales y como complemento de sesiones fotográficas profesionales. Su tamaño lo hace visible e impresionante incluso en espacios grandes.</p>
        <p>Ofrecemos entrega a domicilio en toda la CDMX con el globo inflado y protegido en nuestro vehículo especial. Cada globo se prepara con confeti personalizado en el color que elijas y borlas artesanales de papel de seda para un acabado premium.</p>
      </div>
    </div>
  </section>

  <FaqSection items={faqItems} />

  <section class="cta-dark">
    <div class="container">
      <h2 class="cta-dark__title">¿Listo para reservar?</h2>
      <p class="cta-dark__text">Cotiza tu {paquete.nombre.toLowerCase()} y haz tu evento inolvidable.</p>
      <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener">Cotizar por WhatsApp</a>
    </div>
  </section>
</Base>

<style>
  .features-grid { display: grid; grid-template-columns: 1fr; gap: 0.75rem; max-width: 600px; margin: 0 auto var(--spacing-2xl); }
  .feature-item { display: flex; align-items: center; gap: 0.75rem; padding: 0.875rem 1rem; border-radius: var(--border-radius); font-size: var(--font-size-sm); }
  .feature-item--yes { background: #f0fdf4; color: #166534; }
  .feature-item__icon { font-weight: 700; flex-shrink: 0; }
  .price-highlight { text-align: center; padding: var(--spacing-xl); background: var(--color-bg); border-radius: var(--border-radius-lg); max-width: 500px; margin: 0 auto; display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .price-highlight__label { font-size: var(--font-size-sm); color: var(--color-text-light); text-transform: uppercase; letter-spacing: 0.05em; font-weight: 600; }
  .price-highlight__amount { font-size: 2.5rem; font-weight: 800; color: var(--color-primary); line-height: 1.2; }
  .price-highlight__amount small { font-size: 1rem; font-weight: 500; color: var(--color-text-light); }
  .price-highlight__note { font-size: var(--font-size-sm); color: var(--color-text-light); }
  @media (min-width: 640px) { .features-grid { grid-template-columns: repeat(2, 1fr); } }
</style>
