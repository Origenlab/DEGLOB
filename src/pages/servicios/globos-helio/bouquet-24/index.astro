---
import Base from '../../../../layouts/Base.astro';
import Hero from '../../../../components/Hero.astro';
import Breadcrumb from '../../../../components/Breadcrumb.astro';
import CtaBar from '../../../../components/CtaBar.astro';
import FaqSection from '../../../../components/FaqSection.astro';
import { SERVICIOS, WHATSAPP_URL_MSG, SITE_URL } from '../../../../lib/config';
import { faqSchema } from '../../../../lib/seo';

const servicio = SERVICIOS.find(s => s.slug === 'globos-helio')!;
const paquete = servicio.paquetes.find(p => p.slug === 'bouquet-24')!;
const otrosPaquetes = servicio.paquetes.filter(p => p.slug !== 'bouquet-24');

const faqItems = [
  { pregunta: '¿Qué tipos de globos incluye el bouquet de 24?', respuesta: 'El bouquet de 24 incluye una mezcla de globos de latex con helio y globos foil (metálicos) en formas de estrellas, corazones o números. Esta combinación crea un arreglo más vistoso y variado.' },
  { pregunta: '¿Se puede agregar un mensaje personalizado?', respuesta: 'Sí, podemos agregar globos con mensajes impresos como "Feliz Cumpleaños", "Te Amo" o "Felicidades". También podemos incluir globos con números de edad o iniciales.' },
  { pregunta: '¿Incluyen instalación del bouquet de 24?', respuesta: 'Sí, este paquete incluye entrega e instalación. Colocamos los bouquets en las áreas que nos indiques, distribuyéndolos estratégicamente para lograr el mejor efecto decorativo.' },
  { pregunta: '¿Puedo dividir los 24 globos en varios bouquets?', respuesta: 'Sí, podemos dividirlos en 2, 3 o 4 bouquets más pequeños para decorar diferentes mesas o áreas de tu evento. Cada mini-bouquet incluye su propio peso decorativo.' },
  { pregunta: '¿Las cintas premium en qué se diferencian?', respuesta: 'Las cintas premium son de raso o organza en colores coordinados, a diferencia de las cintas estándar de plástico. Agregan un toque de elegancia y sofisticación al arreglo flotante.' },
];

const structuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: `${paquete.nombre} — ${servicio.nombre}`,
    description: `Paquete ${paquete.nombre} de ${servicio.nombre.toLowerCase()}. ${servicio.descripcion}`,
    brand: { '@type': 'Organization', name: 'DEGLOB' },
    offers: {
      '@type': 'Offer',
      price: paquete.precio,
      priceCurrency: 'MXN',
      availability: 'https://schema.org/InStock',
      url: `${SITE_URL}/servicios/${servicio.slug}/${paquete.slug}/`,
    },
    aggregateRating: {
      '@type': 'AggregateRating',
      ratingValue: '4.9',
      reviewCount: '34',
      bestRating: '5',
    },
  },
  faqSchema(faqItems),
];
---

<Base
  title={`${paquete.nombre} — ${servicio.nombre} | DEGLOB`}
  description={`${paquete.nombre} de ${servicio.nombre.toLowerCase()} desde $${paquete.precio.toLocaleString()} MXN. ${paquete.features.slice(0, 3).join(', ')}. Instalación incluida en CDMX.`}
  structuredData={structuredData}
>
  <Breadcrumb items={[
    { name: 'Servicios', href: '/servicios/' },
    { name: servicio.nombre, href: `/servicios/${servicio.slug}/` },
    { name: paquete.nombre, href: `/servicios/${servicio.slug}/${paquete.slug}/` },
  ]} />

  <Hero
    badge={`${servicio.nombre} — ${paquete.nombre}`}
    title={`${paquete.nombre}`}
    description={`Paquete de ${servicio.nombre.toLowerCase()} que incluye ${paquete.features[0].toLowerCase()}. Instalación profesional incluida en toda la CDMX.`}
    primaryCta={{ text: `Cotizar $${paquete.precio.toLocaleString()} + IVA`, href: WHATSAPP_URL_MSG }}
    secondaryCta={{ text: 'Ver más paquetes', href: `/servicios/${servicio.slug}/` }}
  >
    <Fragment slot="hero-seo">
      <p>El bouquet de 24 globos con helio combina la magia del latex flotante con el brillo de los globos foil metálicos. Con cintas premium y peso decorativo, este arreglo es ideal para decorar cumpleaños, baby showers y celebraciones de aniversario en CDMX.</p>
      <p>La mezcla de texturas entre globos latex y foil crea un efecto visual más rico y elaborado que un bouquet sencillo. Incluye entrega e instalación en toda la Ciudad de México, con la opción de dividir los 24 globos en múltiples bouquets para decorar diferentes áreas de tu evento.</p>
    </Fragment>
  </Hero>

  <CtaBar
    items={otrosPaquetes.map(p => ({ label: p.nombre, href: `/servicios/${servicio.slug}/${p.slug}/` }))}
    headline="Otros paquetes disponibles"
  />

  <section class="section">
    <div class="container">
      <div class="features-grid">
        {paquete.features.map(f => (
          <div class="feature-item feature-item--yes">
            <span class="feature-item__icon">&#10003;</span>
            <span>{f}</span>
          </div>
        ))}
      </div>

      <div class="price-highlight">
        <span class="price-highlight__label">Precio desde</span>
        <span class="price-highlight__amount">${paquete.precio.toLocaleString()} <small>MXN + IVA</small></span>
        <span class="price-highlight__note">Instalación incluida en CDMX</span>
        <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener" style="margin-top:1rem">Reservar Ahora</a>
      </div>
    </div>
  </section>

  <section class="section section--bg">
    <div class="container">
      <div class="prose" style="max-width:800px">
        <h2>Bouquet de 24 Globos con Helio: Decoración Flotante Premium</h2>
        <p>El bouquet de 24 globos con helio lleva la decoración flotante al siguiente nivel. La combinación de globos latex en colores personalizados con globos foil metálicos en formas de estrellas, corazones o números crea un arreglo vistoso y sofisticado que funciona como pieza central de cualquier celebración.</p>
        <p>Las cintas premium de raso o organza agregan una capa de elegancia que eleva el bouquet de una simple decoración a un elemento de diseño. El peso decorativo temático ancla el conjunto mientras permite que los globos dancen suavemente, creando movimiento y vida en el espacio.</p>
        <p>Este paquete es el favorito para decorar mesas principales en cumpleaños de adultos, baby showers en restaurantes de la Condesa, aniversarios en terrazas de Polanco y como complemento de decoraciones más elaboradas en salones de fiesta de CDMX.</p>
      </div>
    </div>
  </section>

  <FaqSection items={faqItems} />

  <section class="cta-dark">
    <div class="container">
      <h2 class="cta-dark__title">¿Listo para reservar?</h2>
      <p class="cta-dark__text">Cotiza tu {paquete.nombre.toLowerCase()} y haz tu evento inolvidable.</p>
      <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener">Cotizar por WhatsApp</a>
    </div>
  </section>
</Base>

<style>
  .features-grid { display: grid; grid-template-columns: 1fr; gap: 0.75rem; max-width: 600px; margin: 0 auto var(--spacing-2xl); }
  .feature-item { display: flex; align-items: center; gap: 0.75rem; padding: 0.875rem 1rem; border-radius: var(--border-radius); font-size: var(--font-size-sm); }
  .feature-item--yes { background: #f0fdf4; color: #166534; }
  .feature-item__icon { font-weight: 700; flex-shrink: 0; }
  .price-highlight { text-align: center; padding: var(--spacing-xl); background: var(--color-bg); border-radius: var(--border-radius-lg); max-width: 500px; margin: 0 auto; display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .price-highlight__label { font-size: var(--font-size-sm); color: var(--color-text-light); text-transform: uppercase; letter-spacing: 0.05em; font-weight: 600; }
  .price-highlight__amount { font-size: 2.5rem; font-weight: 800; color: var(--color-primary); line-height: 1.2; }
  .price-highlight__amount small { font-size: 1rem; font-weight: 500; color: var(--color-text-light); }
  .price-highlight__note { font-size: var(--font-size-sm); color: var(--color-text-light); }
  @media (min-width: 640px) { .features-grid { grid-template-columns: repeat(2, 1fr); } }
</style>
