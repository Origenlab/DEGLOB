---
import Base from '../../../../layouts/Base.astro';
import Hero from '../../../../components/Hero.astro';
import Breadcrumb from '../../../../components/Breadcrumb.astro';
import CtaBar from '../../../../components/CtaBar.astro';
import FaqSection from '../../../../components/FaqSection.astro';
import { SERVICIOS, WHATSAPP_URL_MSG, SITE_URL } from '../../../../lib/config';
import { faqSchema } from '../../../../lib/seo';

const servicio = SERVICIOS.find(s => s.slug === 'globos-helio')!;
const paquete = servicio.paquetes.find(p => p.slug === 'bouquet-12')!;
const otrosPaquetes = servicio.paquetes.filter(p => p.slug !== 'bouquet-12');

const faqItems = [
  { pregunta: '¿Cuánto duran los 12 globos con helio?', respuesta: 'Los globos de latex con helio duran entre 8 y 12 horas. Los globos foil con helio pueden durar varios días. Recomendamos programar la entrega lo más cercano posible al inicio de tu evento.' },
  { pregunta: '¿Puedo elegir los colores de los 12 globos?', respuesta: 'Sí, puedes elegir la combinación de colores que prefieras de nuestra amplia gama de globos latex y foil. Te ayudamos a crear la mezcla perfecta para tu celebración.' },
  { pregunta: '¿El bouquet incluye peso para que no se vuelen?', respuesta: 'Sí, cada bouquet incluye un peso decorativo que mantiene los globos en su lugar. El peso se puede personalizar según la temática del evento.' },
  { pregunta: '¿Ofrecen entrega a domicilio del bouquet?', respuesta: 'Sí, la entrega a domicilio está incluida en el precio en toda la CDMX. Llegamos en el horario que nos indiques con los globos inflados y listos para sorprender.' },
  { pregunta: '¿Para qué ocasiones es ideal el bouquet de 12?', respuesta: 'El bouquet de 12 globos es perfecto para cumpleaños, sorpresas románticas, felicitaciones, recibimientos en hospitales, decoración de restaurantes y como complemento de decoraciones más grandes.' },
];

const structuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: `${paquete.nombre} — ${servicio.nombre}`,
    description: `Paquete ${paquete.nombre} de ${servicio.nombre.toLowerCase()}. ${servicio.descripcion}`,
    brand: { '@type': 'Organization', name: 'DEGLOB' },
    offers: {
      '@type': 'Offer',
      price: paquete.precio,
      priceCurrency: 'MXN',
      availability: 'https://schema.org/InStock',
      url: `${SITE_URL}/servicios/${servicio.slug}/${paquete.slug}/`,
    },
    aggregateRating: {
      '@type': 'AggregateRating',
      ratingValue: '4.9',
      reviewCount: '60',
      bestRating: '5',
    },
  },
  faqSchema(faqItems),
];
---

<Base
  title={`${paquete.nombre} — ${servicio.nombre} | DEGLOB`}
  description={`${paquete.nombre} de ${servicio.nombre.toLowerCase()} desde $${paquete.precio.toLocaleString()} MXN. ${paquete.features.slice(0, 3).join(', ')}. Instalación incluida en CDMX.`}
  structuredData={structuredData}
>
  <Breadcrumb items={[
    { name: 'Servicios', href: '/servicios/' },
    { name: servicio.nombre, href: `/servicios/${servicio.slug}/` },
    { name: paquete.nombre, href: `/servicios/${servicio.slug}/${paquete.slug}/` },
  ]} />

  <Hero
    badge={`${servicio.nombre} — ${paquete.nombre}`}
    title={`${paquete.nombre}`}
    description={`Paquete de ${servicio.nombre.toLowerCase()} que incluye ${paquete.features[0].toLowerCase()}. Instalación profesional incluida en toda la CDMX.`}
    primaryCta={{ text: `Cotizar $${paquete.precio.toLocaleString()} + IVA`, href: WHATSAPP_URL_MSG }}
    secondaryCta={{ text: 'Ver más paquetes', href: `/servicios/${servicio.slug}/` }}
  >
    <Fragment slot="hero-seo">
      <p>El bouquet de 12 globos con helio es el regalo y detalle decorativo más popular para cumpleaños, sorpresas y celebraciones íntimas en la Ciudad de México. Con cintas decorativas y colores personalizados, cada bouquet es una explosión de color que flota con elegancia.</p>
      <p>Incluye entrega a domicilio en toda la CDMX con los globos inflados y listos. Cada globo se llena con helio de alta pureza para garantizar máxima flotación. El peso decorativo incluido mantiene el bouquet en su lugar mientras crea un punto focal festivo en cualquier espacio.</p>
    </Fragment>
  </Hero>

  <CtaBar
    items={otrosPaquetes.map(p => ({ label: p.nombre, href: `/servicios/${servicio.slug}/${p.slug}/` }))}
    headline="Otros paquetes disponibles"
  />

  <section class="section">
    <div class="container">
      <div class="features-grid">
        {paquete.features.map(f => (
          <div class="feature-item feature-item--yes">
            <span class="feature-item__icon">&#10003;</span>
            <span>{f}</span>
          </div>
        ))}
      </div>

      <div class="price-highlight">
        <span class="price-highlight__label">Precio desde</span>
        <span class="price-highlight__amount">${paquete.precio.toLocaleString()} <small>MXN + IVA</small></span>
        <span class="price-highlight__note">Instalación incluida en CDMX</span>
        <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener" style="margin-top:1rem">Reservar Ahora</a>
      </div>
    </div>
  </section>

  <section class="section section--bg">
    <div class="container">
      <div class="prose" style="max-width:800px">
        <h2>Bouquet de 12 Globos con Helio: Color Flotante para tu Celebración</h2>
        <p>Los bouquets de globos con helio son una de las formas más encantadoras de agregar magia a cualquier celebración. Con 12 globos flotantes en los colores de tu elección, este arreglo crea un punto focal festivo que alegra cualquier espacio, desde una sala de estar hasta un salón de fiestas.</p>
        <p>Cada globo se infla con helio de alta pureza y se sella profesionalmente para garantizar horas de flotación. Las cintas decorativas se cortan a la medida ideal para crear un efecto de cascada elegante. El peso incluido mantiene todo en su lugar mientras permite que los globos se muevan suavemente con la brisa.</p>
        <p>Este bouquet es el más solicitado para entregas sorpresa en cumpleaños, decoración de mesas principales en restaurantes, recibimientos de bebés en hospitales y como complemento de decoraciones más elaboradas en eventos de CDMX.</p>
      </div>
    </div>
  </section>

  <FaqSection items={faqItems} />

  <section class="cta-dark">
    <div class="container">
      <h2 class="cta-dark__title">¿Listo para reservar?</h2>
      <p class="cta-dark__text">Cotiza tu {paquete.nombre.toLowerCase()} y haz tu evento inolvidable.</p>
      <a href={WHATSAPP_URL_MSG} class="btn btn--accent" target="_blank" rel="noopener">Cotizar por WhatsApp</a>
    </div>
  </section>
</Base>

<style>
  .features-grid { display: grid; grid-template-columns: 1fr; gap: 0.75rem; max-width: 600px; margin: 0 auto var(--spacing-2xl); }
  .feature-item { display: flex; align-items: center; gap: 0.75rem; padding: 0.875rem 1rem; border-radius: var(--border-radius); font-size: var(--font-size-sm); }
  .feature-item--yes { background: #f0fdf4; color: #166534; }
  .feature-item__icon { font-weight: 700; flex-shrink: 0; }
  .price-highlight { text-align: center; padding: var(--spacing-xl); background: var(--color-bg); border-radius: var(--border-radius-lg); max-width: 500px; margin: 0 auto; display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .price-highlight__label { font-size: var(--font-size-sm); color: var(--color-text-light); text-transform: uppercase; letter-spacing: 0.05em; font-weight: 600; }
  .price-highlight__amount { font-size: 2.5rem; font-weight: 800; color: var(--color-primary); line-height: 1.2; }
  .price-highlight__amount small { font-size: 1rem; font-weight: 500; color: var(--color-text-light); }
  .price-highlight__note { font-size: var(--font-size-sm); color: var(--color-text-light); }
  @media (min-width: 640px) { .features-grid { grid-template-columns: repeat(2, 1fr); } }
</style>
